<!-- See and update existing burger info -->
<div class="container">
  <div class="row">
    <div class="col-xs-12 text-center" id="top-bar">
      <!-- Page Header -->
      <h1>BURGER APP!</h1>
    </div>
    <div class="col-xs-6 text-center" class="task">
      <div class="row">
        <!-- Lines 10 through 26. Creating a page that will loop through burger data (each row in db) and for each item, it will run this.id and this.burger_name within a pre-formatted text element and it will create a button that, upon click, will send this.id back to the server. All of this will happen unless this.devoured is true.  -->
        {{#each burger_data}}
          {{#unless this.devoured}}
        <div class="col-xs-9 text-center">
          <!-- pre-formatted text element -->
          <pre>{{this.id}}. {{this.burger_name}}</pre>
        </div>
      <div class="col-xs-3 text-center">
        <form method="POST" action="/burgers/updateSetWhere?_method=PUT" class="button-size">
          <input type="hidden" name="burger_id" type="text" value={{this.id}}>
          <br>
          <button type="submit" class="btn btn-default">DEVOUR THE BURGER!</button>
        </form>
      </div>
        {{/unless}}
        {{/each}}
      </div>
    </div>
    <div class="col-xs-6 text-center devoured_burger_class" class="task">
      <p>Burgers that have already been devoured!</p>
      {{#each burger_data}}
        <!-- If this.devoured is true, we are going to create a button that is read only and has the id and burger name of what we are passing through. -->
        {{#if this.devoured}}
          <!-- using placeholder and read only so that the text will automatically appear and cannot be alterd on the webpage -->
          <input class="form-control placeholder_boxes " type="text" placeholder="{{this.id}}. {{this.burger_name}}" readonly>
        {{/if}}
      {{/each}}
    </div>
  </div>
</div>

<!-- Add burger section -->
<div class="container">
  <div class="row">
    <div class="col-xs-12 text-center enter_section">
    <p>Enter the name of a burger that you would like to eat!</p>
    <form action="/burgers/insertIntoValues" method="POST">
      <input type="text" name="burger_name" class="form-control" value="" id="enter_text">
      <button type="submit" class="btn btn-primary" id="text-enter-button">ADD NEW BURGER!</button>
    </form>
    </div>
  </div>
</div>

<!-- Burger image section -->
<div class="container">
  <div class="row">
    <div class="col-xs-12 text-center" id="burger_img">
      <img src="assets/img/burger.jpg">
    </div>
  </div>
</div>